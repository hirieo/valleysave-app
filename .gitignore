# Paquete de traspaso Mac (contiene secretos en claro)
/valleysave-mac-transfer/

# macOS
.DS_Store

# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
*.deb
ios/Pods/
ios/.symlinks/
ios/Flutter/Flutter.framework
ios/Flutter/Flutter.podspec
ios/Flutter/Generated.xcconfig
ios/Flutter/flutter_export_environment.sh
android/.gradle/
android/captures/
android/gradlew
android/gradlew.bat
android/local.properties
android/key.properties
android/keystore_password.txt
# Mismos archivos si aparecen en la raíz (restos de transferencia entre
# máquinas) — el patrón de arriba solo cubre android/.
/key.properties
/keystore_password.txt
android/**/GeneratedPluginRegistrant.java
**/ios/Runner/GeneratedPluginRegistrant.*
**/Flutter/.last_build_id

# Plataformas desktop
macos/Flutter/ephemeral/
windows/flutter/ephemeral/
linux/flutter/ephemeral/

# Coverage / tests
coverage/
test/.test_coverage.dart

# IDE
.idea/
.vscode/
*.iml
*.iws
*.ipr

# Windows / OS
Thumbs.db
Desktop.ini
ehthumbs.db

# Secretos y credenciales
.env
.env.local
.env.*.local
*.key
*.pem
*.pfx
*.p12
*.keystore
*.jks
google-services.json
GoogleService-Info.plist
oauth-token.json

# Logs
*.log
logs/

# Binarios de release (se suben a GitHub Releases, no al repo)
dist/

# Plans (work-in-progress, no van al repo público)
plan/

# Credenciales OAuth — nunca al repo
.credenciales/

# Claude Code — configuración interna
.claude/
.agents/
.Codex/
.codex/
.specify/
specs/
.metadata
CLAUDE.md
AGENTS.md
PROMPT.md
.claudeignore

# Investigación y planes auxiliares de agentes; no forman parte del producto.
docs/host-swap-research/
docs/plan-compartir-saves-coop.md

# Mockups y capturas de dev
mockup_*.html
mockup_v*.html
season_options.html
flutter_*.png
run.ps1

# Graphify y previews — artefactos locales, nunca al repo
graphify-out/
lib/graphify-out/
.preview/

# Builds de release — van a GitHub Releases, no al repo
ValleySave-*.apk
ValleySave-*.zip

# Binarios empaquetados localmente; se publican como artefactos/releases.
dist/

# Redistributable de VC++ para el instalador Windows — se descarga en build
# (https://aka.ms/vs/17/release/vc_redist.x64.exe), no va al repo (25MB).
windows/packaging/vc_redist.x64.exe
